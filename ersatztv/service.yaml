apiVersion: v1
kind: Service
metadata:
  name: erzatztv-service    # < name of the service
  namespace: erzatztv # < namespace where place the deployment and pods
spec:
  selector:
    app: erzatztv # < reference to the deployment (connects with this deployment)
  ports:
    - name: webinterface
      protocol: TCP
      port: 8409
