apiVersion: apps/v1
kind: Deployment
metadata:
  name: influxdb
spec:
  selector:
    matchLabels:
      app: influxdb
      release: influxdb
  template:
    metadata:
      labels:
        app: influxdb
        release: influxdb
    spec:
      volumes:
      - name: influxdb-volume
        hostPath:
          path: /mnt/k3s/influxdb
      containers:
      - image: influxdb
        imagePullPolicy: Always
        resources:
          limits:
            nvidia.com/gpu: 1
        name: influxdb
        ports:
        - containerPort: 8086
          name: http
          protocol: TCP
        volumeMounts:
          - name: influxdb-volume
            mountPath: /var/lib/influxdb2
        env:
        - name: PUID
          value: "3000"
        - name: PGID
          value: "3000"
        - name: NVIDIA_VISIBLE_DEVICES
          value: all
        - name: NVIDIA_DRIVER_CAPABILITIES
          value: all
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
